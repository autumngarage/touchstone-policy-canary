schema = 1

[validation]
runtime = "bash"

[[validation.targets]]
name = "root"
path = "."

[[validation.tasks]]
name = "new-source-before-rollback"
target = "root"
command = "printf new-source-before-rollback"
required = true
